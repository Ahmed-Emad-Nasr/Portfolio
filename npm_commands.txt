# تطوير
npm run dev              # تشغيل الموقع + مراقبة الصور وضغطها تلقائياً عند الإضافة/التعديل
npm start               # تشغيل الموقع بعد البناء

# البناء والفحص
npm run build           # بناء المشروع
npm run lint            # فحص الأخطاء والتحذيرات
npm run type-check      # فحص أخطاء TypeScript بدون بناء
npm run format          # تنسيق الكود تلقائياً
npm run format:check    # فحص مشاكل التنسيق بدون تعديل

# التنظيف والأمان
npm run clean           # حذف مجلدات البناء (.next و out)
npm run audit           # فحص الثغرات الأمنية

# فحص شامل
npm run verify          # يشغل: lint + type-check + build

# ضغط الصور (Lossless)
npm run images:compress:lossless:dry # تجربة الضغط بدون كتابة ملفات
npm run images:compress:lossless     # ضغط الصور فعليًا بدون فقدان جودة
npm run images:watch                 # مراقبة مجلد الصور وضغط أي صورة جديدة/معدلة تلقائياً

# ضغط ملفات PDF
npm run pdfs:compress:dry            # تجربة ضغط الـ PDF بدون كتابة ملفات
npm run pdfs:compress                # ضغط ملفات PDF داخل public
npm run pdfs:watch                   # مراقبة ملفات PDF وضغط أي ملف جديد/معدل تلقائياً

# تثبيت المتعلقات
npm install             # تثبيت جميع الـ packages