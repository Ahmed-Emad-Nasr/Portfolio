# التعديلات المطبّقة — الجولة التانية

كل اللي تحت متعمّل فعلاً في `app/` المرفق. اتأكدت منه بـ:

```
npx tsc --noEmit    → نضيف
npx eslint .        → نضيف
npx next build      → نجح، 46 صفحة اتولّدت
```

مسّيتش أي مسار.

---

## أول حاجة: تعارض بين الـ CV والموقع

قبل أي فيتشر، ده الأهم. الأرقام دي **مختلفة** في المكانين، وأي حد هيقرا
الاتنين هيلاحظ. وده بيضر أكتر من أي مكسب تقني في الملف ده كله.

| البند | الموقع | الـ CV |
|---|---|---|
| ترتيبك في الدفعة | **5th** من 900 | **8th** من 900 |
| تنبيهات DEPI | **150+** alert | **30+** alert |
| نفس الرقم في الـ hero | **200+** simulated alerts | — |
| معمل الكشف | زوّد التغطية **12%** | زوّد إثراء اللوجات **25%** |
| طلبة iSchool | **150+** | **470+** |
| مسمى DEPI | SOC Analyst Intern | Information Security & Incident Response Analyst Intern |

**مسّيتش ولا رقم منهم.** دي أرقامك إنت وإنت اللي تعرف الصح. بس لازم يتوحّدوا
قبل أي حاجة تانية — خصوصاً ترتيب الدفعة، لأنه رقم قابل للتحقق من الجامعة.

**وكمان لينكاتك مختلفة في تلات أماكن:**

```
الـ CV:      linkedin.com/in/ahmed-emad     github.com/Ahmed-Emad     ahmedemad.dev
الموقع:      linkedin.com/in/ahmed-emad-nasr  github.com/Ahmed-Emad-Nasr
الـ README:  linkedin.com/in/0x3omda
```

لو اللي في الـ CV مش مختصر للعرض، فده معناه إن لينكات الـ CV بتوديك على
404. و`ahmedemad.dev` مش الدومين المنشور — اشتريته ولسه؟

**وحاجات في الـ CV أقوى من اللي على الموقع ومش موجودة عليه:**

- الـ PR اللي دمجته في SOC Fortress — الـ CV بيقول الريبو فيه **1.5k+ stars**.
  ده اللي بيدّي السطر وزنه، والموقع بيقول "merged a PR" وخلاص.
- GDG: الـ CV بيقول الأول **من 250 مدرّب**. الموقع بيقول "1st place" بس.
- Banque Misr: الـ CV فيه "9 gaps" و"6+ remediation actions". الموقع وصفي.
- TheHive مذكور في الـ CV ضمن معمل الـ SOC ومش موجود في تاجات الموقع.

---

## اللي اتضاف

### 1. خريطة تغطية MITRE ATT&CK ⭐

`app/core/config/attack.ts` + `app/components/attack/attack-matrix.tsx`

قسم جديد بالكامل. أعمدة = التكتيكات، خانات = التكنيكات اللي غطّيتها،
والضغط على أي خانة بيفتح التقارير اللي بتثبتها. شدة لون الخانة = عدد
التقارير، فالتغطية العميقة بتبان بنظرة.

الأرقام دلوقتي: **28 تكنيك** عبر **11 تكتيك** من **38 تقرير**.

كله بيتحسب وقت الـ build (`buildCoverage()` على مستوى الموديول)، والفتح
والقفل بـ `<details>` الأصلي — صفر جافاسكريبت للتفاعل، وشغّال بالكيبورد
والـ screen reader بالمجان.

> ⚠️ **راجع `caseAttackMapping` قبل ما تنشر.** الربط مستنتج من عناوين
> وتاجات الـ cases، مش من قراية التقارير — أنا مشوفتش الـ PDFs. غالباً
> فيه تكنيكات غطّيتها ومش مكتوبة، وممكن واحد أو اتنين مش دقيقين. خريطة
> تغطية غلط أسوأ من عدم وجودها، لأن اللي هيقراها هيفتح التقرير ويقارن.

### 2. قسم تواصل حقيقي

`app/components/contact/contact-section.tsx`

الموقع كان مفيهوش فورم خالص — أيقونات سوشيال و`mailto:` بس، والـ `mailto:`
بتفشل صامتة على أي موبايل مفهوش mail client متظبط.

- Formspree مباشر بـ `fetch` (الـ static export مفيهوش سيرفر يستقبل POST)
- كل الحالات مغطّاة: idle / sending / sent / error، مع `aria-live` عشان
  اللي بيستخدم screen reader يسمع النتيجة
- honeypot مخفي بالـ CSS مش بـ `type="hidden"` — البوتات بتتجاهل الـ hidden
- **لو `NEXT_PUBLIC_FORMSPREE_ENDPOINT` مش موجود، الفورم مبيتعرضش أصلاً**
  وبيتعرض مكانه طرق التواصل المباشرة. أسوأ حاجة ممكن تحصل هي فورم بيبتلع
  الرسايل من غير ما حد ياخد باله.

كمان: `id="Contact"` كان محطوط على صف أيقونات السوشيال في الـ hero، والـ
JSON-LD في `layout.tsx` و`page.tsx` بيشاور عليه. اتنقل للقسم الجديد —
عنصرين بنفس الـ id مش HTML صالح والمتصفح بيقف عند أول واحد.

### 3. الشهادات والجوايز والمهارات كبيانات

`config/certifications.ts` · `config/achievements.ts` · `config/skills.ts`
+ `components/credentials/`

بتتعرض فوق حيطة الصور في نفس قسم Certifications، فمفيش id جديد ولا تغيير
في الـ scrollspy.

- **الشهادات:** 8 شهادات بالاسم والجهة والسنة (6 من الـ CV + HCIA من
  الـ timeline). قبلها كانت 74 صورة كلها `alt="Certification"`.
- **الجوايز:** Top 1% على TryHackMe، الأول من 250 مدرّب، 44/450 في CTF،
  Top Achiever في DEPI، Top 99 من 1500 في ECPC، تاني في IEEE GHOSTEC،
  والقناة. **قسم كامل كان في الـ CV ومش موجود على الموقع خالص** — وهو
  أقوى حاجة عندك لأنه تقييم من طرف تالت مش وصف ذاتي.
- **المهارات:** تصنيف الـ CV كامل، وكل مجموعة مربوطة بالـ cases اللي
  بتثبتها. ده اللي بيحوّل قايمة مهارات (ادّعاء) لشبكة أدلة (إثبات).

**ناقص منك حاجتين:** `verifyUrl` لكل شهادة (ده اللي بيخلي السطر دليل مش
ادّعاء)، و`galleryIndex` عشان الشهادة تترابط بصورتها.

### 4. دور NTI كان ناقص من الموقع

`config/experience.ts` — Cybersecurity Instructor في المعهد القومي
للاتصالات، فبراير 2026 لحد دلوقتي. أحدث دور تدريسي عندك ولسه مستمر، ومكانش
موجود على الموقع بأي شكل.

### 5. "0 stars • 0 forks • 0 watchers" على 6 مشاريع من 7

`components/projects/sensei-projects.tsx`

كل الأرقام في `config/projects.ts` مكتوبة صفر بالإيد لأن الـ GitHub fetch
اتشال في نسخة سابقة والسطر فضل. فالموقع كان بيطبع تحت عنوان اسمه
**"Open-source impact"**:

```
Open-source impact: 0 stars • 0 forks • 0 watchers.
```

يعني السطر اللي المفروض يبيع الشغل كان بيقول إن الشغل ملوش تأثير.

- الشارات (⭐ 🍴 👁) بتظهر بس لما فيها رقم فعلاً
- سطر الـ Result بقى بيتكلم عن اللي اتعمل بدل اللي اتقاس لما مفيش أرقام
- Threat-Intelligence-Tool لسه بيعرض الـ 10 stars بتوعه عادي

لو عايز الأرقام الحقيقية: هاتها من GitHub API في GitHub Action وقت الـ
build واكتبها في `config/projects.ts`. مفيش داعي لأي fetch من المتصفح.

### 6. الـ Structured Data

`layout.tsx` — الـ `Person` schema بقى فيه:

- `hasCredential` — كل شهادة كـ `EducationalOccupationalCredential`
- `knowsAbout` — كل المهارات، وده الحقل اللي جوجل بيفهم بيه مجال الشخص
- `award` — كل الجوايز

مصدر واحد بيغذّي العرض والـ JSON-LD مع بعض، فمستحيل يفترقوا.

### 7. الـ nav والاختصارات

القسمين الجديدين اتضافوا لـ: قايمة الهيدر (`ATT&CK` و`MAIL`)، شريط التقدم،
الـ scrollspy، واختصارات `g`+حرف في الـ command palette (`g a` للخريطة،
`g m` للتواصل).

---

## اللي لسه محتاج منك

1. **وحّد الأرقام** بين الـ CV والموقع (الجدول فوق).
2. **راجع `caseAttackMapping`** في `config/attack.ts` من التقارير الأصلية.
3. **`verifyUrl`** لكل شهادة، و**بروفايل TryHackMe** في `achievements.ts` —
   دول اللي بيحوّلوا الادّعاءات لأدلة.
4. **`NEXT_PUBLIC_FORMSPREE_ENDPOINT`** في بيئة الـ deploy عشان الفورم يشتغل.
5. **الـ `srcset` للصور** — لسه أكبر مكسب أداء متبقّي، ومحتاج تشغّل سكربت
   الـ Python الأول.

---

# جولة تالتة — شكل وأداء

`tsc` نضيف · `eslint` نضيف · `next build` نجح (46 صفحة).

## الشكل

### 1. خط JetBrains Mono مكانش بيتحمّل أصلاً 🔴

`globals.css` فيه:

```css
--font-mono: "JetBrains Mono", monospace;
```

ومفيش أي حاجة في المشروع بتحمّل الخط ده. `layout.tsx` بيحمّل Overlock بس.

عدّيت على الـ CSS: **70 تصريح** بيطلب `--font-mono`. الـ HUD بتاع الـ hero،
شارات المشاريع، الترمينال، الـ command palette، كل التواريخ والأرقام،
وعناوين التكتيكات الجديدة.

كلهم كانوا بيرجعوا للـ monospace بتاع النظام:

| الجهاز | الخط اللي كان بيتعرض فعلاً |
|---|---|
| ويندوز | Courier New |
| ماك | SF Mono / Menlo |
| أندرويد | Roboto Mono |
| لينكس | DejaVu Sans Mono |

يعني الحتة اللي الهوية البصرية كلها قايمة عليها — الشكل الـ terminal —
كانت بتتعرض بأربع خطوط مختلفة حسب جهاز الزائر، وعلى ويندوز (أغلب الزوار)
بـ Courier New.

اتحمّل دلوقتي بـ `next/font/google` بوزنين بس (400 و 700). وكمان **18 تصريح**
كانوا مكتوبين `font-family: monospace` مباشرة، بيتخطّوا المتغيّر خالص —
اتوحّدوا كلهم.

### 2. النص الياباني مكانش ليه خط 🔴

```css
--font-jp: "Overlock", sans-serif;
```

**Overlock مفيهوش ولا حرف ياباني.** فكل الياباني على الموقع — 武士道 في
الـ dividers، セキュリティ・アナリスト في الـ hero، 経験 و 計画 و 認定資格
في عناوين الأقسام — كان بيرجع لأي خط CJK موجود على الجهاز: Yu Gothic على
ويندوز، Hiragino على ماك، Noto على أندرويد. أوزان وعروض مختلفة تماماً لنفس
الهوية، ومربّعات فاضية على أي جهاز مفهوش خط CJK.

وأسوأ من كده: `--font-jp` كان متطبّق على `.vertical-text` بس. باقي الياباني
مكانش حتى بيوصله المتغيّر.

- `--font-jp` بقى stack نظام مضبوط لكل منصة (صفر بايت تحميل)
- قاعدة `[lang="ja"], :lang(ja)` بتطبّقه على **كل** نص ياباني
- `lang="ja"` اتضاف على نص الـ KanjiDivider — ده كمان بيمنع الـ screen
  reader إنه ينطق الكانجي بنطق إنجليزي

> لو عايز تحكّم كامل: Noto Sans JP بـ `next/font/local` من subset فيه
> الحروف المستخدمة بس (معدودة وثابتة) = ~20KB بدل 1.5MB.

### 3. طبقة تفاصيل — كلها CSS، صفر جافاسكريبت

| الحاجة | الفرق |
|---|---|
| `::selection` | كان أزرق النظام، بيقطع الهوية أول ما حد يعلّم على سطر |
| `scroll-margin-top` | لينك `#Contact` من بره الموقع، وزرار الرجوع، والـ palette — كلهم كانوا بيودّوا القسم تحت الهيدر الثابت. الـ nav كان بيعالجها بجافاسكريبت، والباقي لأ |
| `text-wrap: balance` | العناوين ماتسيبش كلمة وحيدة في آخر سطر — أوضح فرق على الموبايل |
| `text-wrap: pretty` | نفس الحاجة للفقرات، بتكلفة أقل |
| `tabular-nums` | الأرقام والتواريخ تتحاذى في العواميد بدل ما "111" تبقى أضيق من "999" |
| شريط التمرير | كان أبيض عريض على ويندوز جنب تصميم غامق |
| `@media print` | **مكانش فيه ولا سطر.** بورتفوليو بيتطبع فعلاً: Save as PDF قبل إنترفيو. من غير ده كان بيطلع خلفية سودا، وكل اللينكات نص من غير عناوين |

طباعة الموقع دلوقتي بتطلّع نسخة نضيفة: خلفية بيضا، الزخرفة كلها مختفية،
وكل لينك خارجي بيطبع عنوانه جنبه.

---

## الأداء

### 4. جافاسكريبت الصفحة الرئيسية: 935KB ← 824KB

```
قبل:  935 KB عبر 19 ملف
بعد:  824 KB عبر 16 ملف     (-12%، غير مضغوط)
```

تلات مصادر:

**(أ) داتا البلوج كانت بتتحمّل على الصفحة الرئيسية — 81 كيلوبايت**

خريطة ATT&CK وقسم الشهادات الاتنين كانوا `"use client"` وبيعملوا import
لـ `caseEvidenceLibrary` عشان يقروا عناوين التقارير. أي حاجة client
component بيستوردها بتتحزم في bundle المتصفح — يعني الـ 42 كيلوبايت بتاعة
مكتبة الـ cases كانت بتتحمّل على كل زائر للصفحة الرئيسية، عشان عناوين
اتحوّلت لـ HTML وقت الـ build أصلاً.

- **خريطة ATT&CK بقت Server Component.** `page.tsx` بيرندرها وبيبعتها
  لـ `page-client` كـ prop. الداتا بتفضل وقت الـ build والزائر بياخد
  markup بس.
- **قسم الشهادات** بقى بياخد الـ label من `skills.ts` مباشرة بدل ما يدوّر
  في مكتبة الـ cases.

**(ب) `YOUTUBE_CHANNEL_URL` كان بيجرّ موديول اليوتيوب كله — 40 كيلوبايت**

`sensei-home.tsx` محتاج السطر ده بس، وكان مستورده من `config/youtube.ts`
اللي فيه كل مصفوفات الفيديوهات والـ playlists. الـ tree-shaking مبيشيلش
موديول كامل عشان إنت واخد منه ثابت واحد. اتنقل لـ `shared.ts`.

**(ج) `domAnimation` كان static import — ~25 كيلوبايت**

`LazyMotion` بيقبل دالة بترجّع Promise. بقت بتتحمّل بعد الـ paint الأول بدل
ما تأخّره، وده بيفيد صفحات البلوج كمان.

**التأكيد:** بعد التعديل، فحص chunks الصفحة الرئيسية مبيلاقيش ولا عنوان
case ولا أي داتا يوتيوب.

---

## اللي فاضل، ومقدّر بالأرقام

| البند | التقدير | ليه مش معمول |
|---|---|---|
| `srcset` للصور | غالباً > كل مكاسب الـ JS مجتمعة | محتاج تشغّل سكربت الـ Python الأول |
| استبدال FontAwesome بـ inline SVG | ~94 KB | refactor في 10+ ملفات. الأيقونات المستخدمة أقل من 25 — SVG مباشر يخليها ~3 KB |
| Noto Sans JP كـ subset | تناسق كامل للياباني | محتاج تولّد ملف الـ subset بـ `pyftsubset` |

التلات chunks الكبار الباقيين (222 + 150 + 110 KB) هما React و React DOM
وruntime بتاع Next. دول مفيش فيهم شغل.

---

# جولة رابعة — إصلاح شكل السكاشن الجديدة

من الصور: المشكلة الأساسية إن التلات سكاشن الجديدة كانت **بتتعرض بقواعد
تصميم مختلفة عن باقي الموقع**، فشكلها كإنها ملزوقة من موقع تاني.

## 1. الهيدرات كانت محاذية لليسار والموقع كله متوسّط

كل الأقسام الموجودة (Experience, Projects, Certifications) بتستخدم نفس
الهيكل: `SectionHeader` بعنوان متوسّط تحته خط أحمر، جوّه
`.header-section { text-align: center }`.

السكاشن الجديدة كتبت هيدر خاص بيها محاذي لليسار. اتوحّدوا دلوقتي على نفس
الكومبوننت ونفس تعريف `.title` بالحرف — نفس الحجم ونفس الـ letter-spacing
ونفس الخط اللي تحته.

## 2. قسم الشهادات كان بيلزق في حواف الشاشة

`.container` بتاع قسم الـ Certifications معرّف كده:

```css
.container { width: 100%; max-width: none; margin: 0 auto; }
```

**من غير أي padding.** الـ padding موجود على `.art-gallery-content` بس،
واللي فيه حيطة الصور. فلما حطّينا الشهادات جنبها، الكتلة اتمدّت على عرض
الشاشة كامل من غير هوامش.

`.wrap` دلوقتي بيعمل الـ container بنفسه بنفس التوكنز
(`--container-pad-x` و `--container-max-width`)، والمحتوى جوّاه محدود
بـ 1100px عشان السطر يتقرا.

## 3. صفوف الشهادات كانت فراغ بشبر في النص

كانت `grid-template-columns: 1fr auto auto auto` على عرض 1500 بكسل: الاسم
في أقصى الشمال، الجهة والسنة في أقصى اليمين، وبينهم فراغ فاضي — العين لازم
تقطع الشاشة كلها عشان تربط الشهادة بجهتها.

بقت كروت في عمودين، كل كارت فيه الاسم والاسم الكامل والجهة والسنة مع بعض.

## 4. الجوايز: 6 أعمدة والسابع يتيم

`minmax(17rem, 1fr)` مع `auto-fit` على 1500px = ستة أعمدة رفيعة وكارت
سابع لوحده في سطر. وكمان حجم العنوان كان بيتغيّر حسب طوله، فكارت "1st place
& Best Technical Instructor" كان مبيّن أكبر من الباقي.

- `minmax(min(100%, 20rem), 1fr)` — تلاتة لأربعة أعمدة كحد أقصى، وسطر
  واحد على الموبايل
- حجم عنوان ثابت (`1.05rem`) لكل الكروت
- `margin-top: auto` على السطر التحتاني فالكروت بتتساوى

## 5. خريطة ATT&CK كانت مقصوصة ورا شريط تمرير

`grid-auto-flow: column` جوه `overflow-x: auto`. النتيجة على الديسكتوب:
آخر عمودين مخفيين، شريط تمرير **رمادي بتاع النظام** في نص التصميم الغامق،
وأسماء تكنيكات مقصوصة (`Network Service Disco…`).

بقت `repeat(auto-fill, minmax(16rem, 1fr))` — الأعمدة بتلف على سطور بدل ما
تختفي. كل التكتيكات الـ 11 ظاهرة على أي عرض، مفيش scroll خالص، والأسماء
بتلف على سطرين بدل ما تتقص.

## 6. رسالة "الفورم مقفول" كانت صندوق خطأ كبير للزائر

كانت `<p>` جوه إطار متقطّع بعرض نص القسم، مكتوب فيها اسم متغيّر بيئة.
الزائر بيقرا رسالة تقنية مالهاش أي معنى بالنسباله، والقسم بيبان مكسور —
نصه محتوى ونصه صندوق أحمر.

- بقت سطر صغير باهت (`opacity: 0.55`) تحت لينكات التواصل
- ولما الفورم مقفول، الـ container بقى عمود واحد متوسّط
  (`[data-form="off"]`) بدل عمودين نصهم فاضي

## 7. حماية من الفراغات الضخمة

`.wrap` بقى فيه `align-content: start`. لو الأب حاول يمدّ الكتلة لطول أكبر
من محتواها، صفوف الـ grid كانت هتتقسّم المساحة الزيادة بينها وتفتح فراغات
بين الشهادات والجوايز والمهارات — وده اللي كان باين في الصورة.

## 8. `lang="ja"` على كل عناوين الأقسام

اتضاف جوه `SectionHeader` نفسه، فكل الأقسام (القديمة والجديدة) بقت تاخد
خط الـ CJK المقصود من قاعدة `:lang(ja)` بدل ما المتصفح يختار أي خط ياباني
لقاه على الجهاز.

---

# جولة خامسة — صفحة CV بـ HTML + مسار قراءة

`tsc` نضيف · `eslint` نضيف · `next build` نجح (**47** صفحة — زادت واحدة).

## 1. صفحة `/cv` بـ HTML

`app/cv/page.tsx` + `app/core/config/cv.ts`

قلت إن الـ CV مش شغال. اللينك القديم كان:

```tsx
<a href="Assets/cv/AhmedEmadNasr_CV.pdf" download>
```

مسار **نسبي**: بيتحل بالنسبة لعنوان الصفحة الحالية. شغّال من `/Portfolio/`
وبيقع من أي مكان أعمق. ولو الملف نفسه ناقص أو اتغيّر اسمه، الزائر بيوصل
لـ 404 من غير أي بديل — وده على الأغلب اللي بيحصل عندك دلوقتي.

الزرار في الـ hero بقى `<Link href="/cv">` (الـ `<Link>` بيتولّى الـ
basePath لوحده زي لينك `/blog` بالظبط، عشان كده مش بيعدّي على
`normalizePublicHref` اللي كان هيضيف البادئة مرتين).

**ليه HTML وإحنا عندنا PDF؟**

- **أنظمة الـ ATS بتقرا HTML أحسن بكتير.** أغلبها بتفشل في استخراج النص من
  PDF فيه أعمدة أو أيقونات وبتطلّع منه خليط مبعثر.
- **جوجل بيفهرسها كصفحة** بترتيب أعلى وتجربة أحسن من PDF.
- **الموبايل.** حد فاتح لينك من لينكدإن مش هيقعد يزوّم في PDF.
- **مستحيل تبقى قديمة.**

آخر نقطة هي الأهم. الصفحة **مبتحتويش ولا سطر منسوخ**: الخبرات من
`experience.ts`، المشاريع من `projects.ts`، الشهادات من
`certifications.ts`، الجوايز من `achievements.ts`، المهارات من
`skills.ts`. الملف الجديد `config/cv.ts` فيه بيانات التواصل والمقرّرات بس —
الحاجات اللي فعلاً مش موجودة في أي مكان تاني.

يعني التعارض اللي بينك وبين الـ PDF دلوقتي (5th مقابل 8th، 150+ مقابل
30+ …) **مستحيل يتكرر** بين الموقع والصفحة دي.

وكمان: `Ctrl+P` على الصفحة بيطلّع PDF نضيف — خلفية بيضا، الزخرفة مختفية،
وكل لينك خارجي بيطبع عنوانه جنبه. يعني عندك مسار لتوليد الـ PDF من نفس
المصدر بدل ما تعدّله بإيدك.

**اتضاف كمان:** `kind: "work" | "education"` على كل عنصر في
`experience.ts`. قبل كده الفرق الوحيد بينهم كان وجود رابط شركة — استنتاج
هش كان هيقع أول ما تضيف عنصر جديد.

## 2. مسار "ابدأ من هنا"

`config/start-here.ts` + `blog/components/StartHere.tsx`

38 تقرير في ليستة واحدة بيشلّوا اللي فاتحها. أغلب اللي بيفتح الصفحة دي حد
بيراجع طلبك وعنده خمس دقايق.

تلات تقارير في أول صفحة البلوج، كل واحد بسطر بيقول ليه تقراه وكام دقيقة.
مختارين عشان يغطّوا تلات قدرات مختلفة: كتابة قواعد كشف، تحقيق DFIR كامل،
وتحليل malware.

> ⚠️ راجع الاختيار. أنا مشوفتش الـ PDFs — الاختيار مبني على العناوين
> والتاجات. إنت اللي عارف أنهي تقرير فعلاً بيمثّلك.

الكومبوننت **Server Component** وبيتبعت لـ `page-client` كـ prop (نفس
أسلوب خريطة ATT&CK)، فمكتبة الـ cases مبتوصلش للمتصفح. وفيه فحص وقت الـ
build: لو `id` مش موجود في `cases.ts`، العنصر بيتشال وبيطلع تحذير في الـ
build بدل لينك 404 صامت.

## 3. `TRAILING_SLASH` — التعارض اللي فاضل من أول تقرير

`config/site.ts` كان فيه `TRAILING_SLASH = true`، والموقع المنشور بيرد على
`/Portfolio/blog` **من غير** سلاش، و`next.config.mjs` مفيهوش `trailingSlash`.

النتيجة إن `sitemap.xml` و `og:url` و JSON-LD `@id` وروابط الـ RSS كلهم
كانوا بيقولوا عناوين بسلاش، بينما الـ canonical واللينكات الحقيقية في
الصفحة من غيره. يعني بتقدّم لجوجل عنوانين مختلفين لنفس الصفحة، والعنوان
اللي في الـ sitemap مش هو اللي بيفتح.

ده مش موضوع مسارات — ده SEO خالص. بقى `false`، والـ sitemap دلوقتي بيطلّع
`…/Portfolio/blog` مطابق للـ canonical. (اتأكدت من الملف المولّد.)

**و`/cv` اتضافت للـ sitemap** بأولوية 0.9 — أعلى من الـ PDF.

## 4. حاجات صغيرة

- `isDownloading` + `handleDownloadClick` اتشالوا من `sensei-home`: مبقاش
  فيه تحميل ملف يستنى مؤشر تحميل. state أقل في أكبر كومبوننت في الصفحة.
- `.sheet [data-print="hide"]` بدل `[data-print="hide"]` — CSS Modules
  بترفض selector من غير class محلي، والـ build كان بيفشل عليها.

---

## اللي لسه مفتوح

| البند | ليه |
|---|---|
| `srcset` للصور | محتاج سكربت الـ Python يشتغل الأول. **لسه أكبر مكسب أداء** |
| FontAwesome → inline SVG | ~94KB. الأيقونات المستخدمة أقل من 25 |
| مصفوفة الأدوات | نفس فكرة ATT&CK بس للتولز. الداتا موجودة في `tools` بتاع كل case |
| بلوكات IOC بزرار نسخ | محتاجة حقل `iocs` على الـ cases، وده منك |
| صفحة `/teaching` | محتاجة قايمة السيشنز والمواد |
| Lighthouse | مشغلتوش ولا مرة — معنديش رقم حقيقي، بس تقديرات |

---

# جولة سادسة — إصلاح الـ Runtime Error + شيل FontAwesome

`tsc` نضيف · `eslint` نضيف · `next build` نجح (47 صفحة) · **`next dev` اتشغّل
واتفحص فعلاً** — كل المسارات 200 وصفر أخطاء.

## 1. الـ Runtime Error — السبب والإصلاح 🔴

```
Page "/blog/[slug]/page" is missing param "/blog/[slug]"
in "generateStaticParams()", which is required with "output: export"
```

شغّلت `next dev` وكررت المشكلة فعلاً قبل ما أصلحها. النتيجة:

```
/                                     200
/blog                                 200
/blog/soc127-pdf                      200
/blog/3omda%20custom%20detection%20rules   404  ← دي
```

**السبب:** `id` واحد في `cases.ts` كان فيه **مسافات**:

```
"3omda custom detection rules"
```

الـ id بيتحوّل مباشرة لمسار الصفحة. اللينك في الصفحة بيتعمله encode
(`%20`)، والـ param الراجع من `generateStaticParams` فيه مسافات فعلية،
فالراوتر مش بيلاقي تطابق ويرمي الخطأ ده.

ده نفس البند اللي كان في أول تقرير بعتهولك ("slug فيه مسافات") — بس
ساعتها كان مشكلة شكل URL، ومع الـ dev server بقى خطأ بيوقف الصفحة.

**الإصلاح:**

```
3omda custom detection rules  →  3omda-custom-detection-rules
```

اتغيّر في `cases.ts` (7 مواضع) و`attack.ts` و`skills.ts` و`start-here.ts`.

**وحارس عشان ميتكررش:** `cases.ts` بقى فيه فحص بيتنفّذ وقت الـ build:

```ts
const SLUG_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
```

أي id مش صالح كـ URL بيفشّل الـ build برسالة واضحة بتقول اسم الـ id
والسبب — بدل ما تكتشفها من صفحة 404. وفيه فحص تاني للـ ids المكررة
(اتنين بنفس الـ id معناهم صفحة بتكتب فوق التانية وقت الـ export).

> ⚠️ العنوان القديم `/blog/3omda%20custom%20detection%20rules` هيبقى 404 لو
> كان متشارك في أي مكان. مستبعد لأنه شكله كان وحش أصلاً، بس خد بالك.

## 2. FontAwesome اتشال بالكامل — 94 كيلوبايت

`@fortawesome/react-fontawesome` + `fontawesome-svg-core` + حزمتين
أيقونات كانوا بيضيفوا **94 كيلوبايت** للـ bundle الأساسي.

الموقع بيستخدم **38 أيقونة**، ومسارات الـ SVG بتاعتها كلها مجتمعة
**21 كيلوبايت**. الباقي كله محرّك: نظام الـ layers والـ transforms
والـ masks، الـ DOM watcher، والـ CSS injection — ولا واحد منهم مستخدم
هنا. الموقع بيرندر أيقونة ثابتة وخلاص.

**الحل:**

```
scripts/generate-icons.cjs      ← بيستخرج المسارات من الحزم
app/core/icons/icon-data.ts     ← مولّد: 38 أيقونة، 21 KB
app/core/icons/Icon.tsx         ← Server Component، ~30 سطر
```

`<Icon>` بيرندر `<svg>` بـ `fill="currentColor"` و`1em` — نفس سلوك
FontAwesome بالظبط: بياخد لون وحجم النص اللي حواليه. وServer Component
عن قصد، فالجدول نفسه مبيوصلش للمتصفح لما يترندر من صفحة سيرفر.

**الخرايط اتغيّرت من كائنات لأسماء:**

```ts
// قبل
const SECTION_ICONS: Record<string, IconProp> = { Home: faHome, … };
// بعد
const SECTION_ICONS: Record<string, IconName> = { Home: "faHome", … };
```

`IconName` نوع مشتق من الجدول المولّد، فاسم أيقونة غلط = خطأ وقت الـ
build مش أيقونة فاضية وقت التشغيل.

**وحزم FontAwesome اتنقلت لـ `devDependencies`** (شوف `PACKAGE-NOTE.md`)
واتضاف `npm run icons`. الحزم لسه متثبّتة عشان السكربت، بس مفيش منها ولا
بايت في اللي بيتشحن للزائر.

## 3. `quality={…}` اتشالت من 5 مواضع

`images.unoptimized: true` معناها إن `quality` مالهاش أي أثر — مفيش
optimizer بيقراها. وNext 16 بقى بيطلّع تحذير عليها:

```
next-image-unconfigured-qualities
```

اتشالت من `BlogCard` و`BlogPdfLibrarySection` و`BlogGalleryModal`
و`CaseArticle`. التحذير اختفى، والكود مبقاش بيوحي بوجود pipeline معالجة
صور مش موجود.

## 4. أرقام الـ bundle بعد كل الجولات

```
الصفحة الرئيسية:  935 KB  →  752 KB   (-20%)
صفحة البلوج:      839 KB  →  768 KB
صفحة /cv:                     652 KB   (جديدة)
```

اللي فاضل (222 + 150 + 110 KB) هو React و React DOM و runtime بتاع Next.
دول مفيش فيهم شغل.

---

## اللي لسه مفتوح — بصراحة

| البند | ليه مش معمول |
|---|---|
| **`srcset` للصور** | محتاج سكربت الـ Python يشتغل الأول. **لسه أكبر مكسب متبقّي، وغالباً أكبر من كل مكاسب الـ JS مجتمعة** |
| Lighthouse | مشغلتوش — كل الأرقام فوق قياسات bundle، مش قياس أداء حقيقي في متصفح |
| مصفوفة الأدوات | نفس فكرة ATT&CK للتولز. الداتا موجودة |
| بلوكات IOC | محتاجة حقل `iocs` على الـ cases — منك |
| صفحة `/teaching` | محتاجة قايمة السيشنز |
| تعارض أرقام الـ CV | قرارك |
| مراجعة `caseAttackMapping` | محتاجة قراية التقارير |
